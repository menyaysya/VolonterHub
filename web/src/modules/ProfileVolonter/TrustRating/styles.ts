import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles((theme) => ({
  title: {
    fontSize: "1.2rem",
    fontWeight: "bold",

  },
  progress: {

    minWidth: "40vh",
    height: "15px",
    borderRadius: "4px",
  },
}));
